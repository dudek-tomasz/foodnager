---
/**
 * Reset Password Page
 * 
 * Password reset page using AuthLayout and ResetPasswordForm component.
 * Features:
 * - Server-side rendering
 * - Token validation from URL
 * - Clean, centered design without navigation
 * - New password input with confirmation
 * - Success state with redirect to login
 */

import AuthLayout from "@/layouts/AuthLayout.astro";
import ResetPasswordForm from "@/components/auth/ResetPasswordForm";

export const prerender = false;

// Get token from URL query params
const token = Astro.url.searchParams.get("token") || "";

// TODO: When implementing backend, add token validation:
// if (!token) {
//   return Astro.redirect("/forgot-password?error=invalid_token");
// }
---

<AuthLayout title="Resetowanie hasła - Foodnager" description="Ustaw nowe hasło do swojego konta">
  <div class="space-y-6">
    <!-- Logo Section -->
    <div class="text-center space-y-2">
      <div class="flex justify-center">
        <span class="text-6xl">🍳</span>
      </div>
      <h1 class="text-3xl font-bold tracking-tight text-neutral-900 dark:text-neutral-100">Foodnager</h1>
      <p class="text-neutral-600 dark:text-neutral-300">
        Ustaw nowe hasło
      </p>
    </div>

    {token ? (
      <!-- Reset Password Form -->
      <ResetPasswordForm client:load token={token} />
    ) : (
      <!-- No Token Error -->
      <div class="bg-red-50 dark:bg-red-950/30 border border-red-200 dark:border-red-900 rounded-lg p-6 text-center space-y-4">
        <div class="flex justify-center">
          <svg
            class="h-12 w-12 text-red-600 dark:text-red-400"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width={2}
              d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"
            />
          </svg>
        </div>
        <div class="space-y-2">
          <h2 class="text-lg font-semibold text-red-900 dark:text-red-100">
            Nieprawidłowy link
          </h2>
          <p class="text-sm text-red-700 dark:text-red-300">
            Link do resetowania hasła jest nieprawidłowy lub wygasł.
          </p>
          <p class="text-xs text-red-600 dark:text-red-400 mt-4">
            <a href="/forgot-password" class="underline hover:no-underline">
              Wyślij nowy link resetujący
            </a>
          </p>
        </div>
      </div>
    )}
  </div>
</AuthLayout>

